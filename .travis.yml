language: c

matrix:
  include:
    - os: linux
    - os: osx
      env:
        - CFLAGS="-arch x86_64"
    - os: osx
      env:
        - CFLAGS="-arch i386"

script:
- make

#addons:
#  apt:
#    packages:
#      - gcc-mingw-w64-i686
